# Dependencies are defined in pyproject.toml — the single source of truth.
# This installs the regin package (editable) plus all its dependencies.
# Equivalent to: pip install -e .
#
# Optional extras live elsewhere:
#   - dev tooling:        pip install -e ".[dev]"
#   - launching agents:   pip install -e ".[agent-sdk]"
#   - SkillRouter (heavy): pip install -r requirements-router.txt
-e .
