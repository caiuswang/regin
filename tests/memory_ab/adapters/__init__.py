"""System adapters: each ingests the shared corpus and emits a result dump
in the common shape (see ../spec.py). `regin.py` is fully scriptable;
the Hindsight side is an agent-driven runbook (see ../HINDSIGHT_RUNBOOK.md).
"""
