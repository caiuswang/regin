{"query": "A Vue settings panel renders with the right class names but no styling — only the child component is affected. What's going on?", "expect_any": ["scoped styles", "style scoped", "shared classes must be global"], "intent": "debug", "note": "Vue scoped-styles gotcha (lesson/working) — debug phrasing on a child-only unstyled symptom."}
{"query": "My test function is just a flat list of asserts and it's tripping the complexity gate even though there's no branching. How do I fix it?", "expect_any": ["radon counts asserts", "each `assert` statement as a decision point", "collapse repeated same-shape asserts"], "intent": "debug", "note": "radon-asserts-toward-CC lesson — debug intent on a CC gate failure."}
{"query": "I want a separate SQLite database with its own SQLModel tables, but create_all keeps building the whole app schema into it.", "expect_any": ["SQLModel subclasses do NOT get their own metadata", "metadata = MetaData()", "global SQLModel.metadata"], "intent": "build", "note": "SQLModel-metadata-is-global lesson (episodic) — build intent on a separate-DB task."}
{"query": "How should I give a short-lived UserPromptSubmit hook dense + rerank recall without loading the embedder every prompt?", "expect_any": ["borrow the already-warm", "regin serve", "loopback", "warm-server"], "intent": "plan", "note": "Warm-server-borrow lesson (episodic) — plan/design intent on a recall-architecture question."}
{"query": "What's the design reference regin's agent memory borrows its ports-and-adapters decoupling from?", "expect_any": ["mnemopi", "oh-my-pi"], "intent": "explain", "note": "mnemopi fact (episodic) — explain intent, fact kind."}
{"query": "To reproduce exactly what the trace UI renders for a session, which function should I call?", "expect_any": ["merge_spans", "_graft_orphans"], "intent": "explain", "note": "Serve-time-trace-path fact (working) — explain intent on a reproduction question."}
{"query": "A failed tool call isn't showing up when I filter spans by name. Why am I missing some failures?", "expect_any": ["tool.failure", "two span shapes", "tool_name"], "intent": "debug", "note": "tool.failure-vs-tool.<Name> lesson — debug intent on a missing-failure-span symptom."}
{"query": "I'm about to grant a subprocess judge new capabilities via allowedTools. Any gotcha with the glob syntax?", "expect_any": ["allowedTools", "non-equivalent glob", "command-name prefix", "unrestricted shell"], "intent": "build", "note": "allowedTools-glob lesson — build intent on a capability-granting task."}
{"query": "Best way to start investigating a weird session trace or the trace DB?", "expect_any": ["regin's purpose-built tooling", "guessed column", "sqlite3.connect"], "intent": "plan", "note": "Start-investigations-from-regin-tooling lesson — plan intent on a how-to-begin question."}
{"query": "Building a live pending span for a tool call — what attribute shape should it carry so the card renders?", "expect_any": ["flat attribute keys", "tool_input dump", "command_preview"], "intent": "build", "note": "Pending-spans-need-flat-attrs lesson — build intent on a span-construction task."}
{"query": "New memories are not being found by semantic search until someone runs reflect", "expect_any": ["Embed coverage", "embed_coverage", "stale_embedding_todo"], "intent": "debug", "note": "G1 embeddings aspect — fresh-lesson dense visibility; added after the 10-aspect verification round."}
{"query": "Running reflect decayed memories that were still useful", "expect_any": ["trim", "_VALIDATIONS_KEEP", "threshold rule"], "intent": "debug", "note": "G10 reflect aspect — decay-rule mechanics; added after the 10-aspect verification round."}
{"query": "The trace UI shows the wrong recall mode on memory injection spans", "expect_any": ["probe end-to-end", "memory.recall span", "mode"], "intent": "debug", "note": "G4 trace-span aspect — inject observability; added after the 10-aspect verification round."}
{"query": "Add cue words so prompts like 'investigate the flaky test' route to the debug intent in memory recall", "expect_any": ["Intent cue traps", "prefix containment", "bare verbs"], "intent": "build", "note": "G5 intent aspect — cue-tuning traps; added after the 10-aspect verification round."}
