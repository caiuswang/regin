{"query": "My raw sqlite SELECT against session_spans just threw OperationalError no such column — what do I check before writing the query?", "expect_any": ["PRAGMA table_info", "schema drift", "confirm column names"], "intent": "debug", "note": "debug->gotcha vs tooling-lesson near-tie (sqlite column); both memories mention sqlite/no-such-column so kind weight is the tiebreak."}
{"query": "What's the external project regin's agent-memory architecture is modeled on?", "expect_any": ["mnemopi", "oh-my-pi"], "intent": "explain", "note": "explain->fact; fact kind in a lesson-dominated corpus."}
{"query": "I'm adding a brand-new type:system subtype and need every place to wire it so the span isn't dropped — give me the steps.", "expect_any": ["_SYSTEM_EVENT_EMITTERS", "three layers", "_TRACED_SYSTEM_SUBTYPES"], "intent": "build", "note": "build->procedure vs twin lesson (system subtype); near-duplicate content, procedure should win for build."}
{"query": "What's the right way to begin poking around an unfamiliar session trace and its DB?", "expect_any": ["purpose-built tooling", "guessed", "regin's own"], "intent": "plan", "note": "plan->episodic tooling-lesson over the working sqlite gotcha; NON-SAT in fts (OFF rank 3 -> ON rank 2)."}
{"query": "Auto-injected memories sometimes silently degrade to lexical recall in production — how do I even detect that?", "expect_any": ["probe end-to-end", "falls back to FTS", "inject_server_timeout"], "intent": "debug", "note": "all-lesson cluster: routing can't boost target's kind, but demotes wrong-kind distractors. NON-SAT in fts (2 -> 1)."}
{"query": "How can a per-prompt hook get reranked dense recall without paying the embedder load each time?", "expect_any": ["warm", "regin serve", "loopback"], "intent": "plan", "note": "all-lesson cluster; control — routing largely inert here."}
{"query": "Why are freshly-distilled memories invisible to semantic search until reflect runs?", "expect_any": ["embed", "coverage", "recallable"], "intent": "debug", "note": "all-lesson cluster; NON-SAT in fts but routing did not move it (2 -> 2) — honest null case."}
{"query": "A tool call that failed isn't turning up when I filter spans — what am I missing?", "expect_any": ["tool.failure", "two span shapes", "tool_name"], "intent": "debug", "note": "span-failure cluster; NON-SAT in fts (2 -> 1)."}
{"query": "A pending live span renders blank in the card until it resolves — what attribute shape does it need?", "expect_any": ["flat attr", "command_preview", "same flat attribute keys"], "intent": "build", "note": "pending-span cluster; NON-SAT in fts (2 -> 1)."}
{"query": "A correct, verified fix got graded needs_revision — where does that usually come from?", "expect_any": ["evidence selection", "evidence-*selection*", "stale", "load_bearing"], "intent": "debug", "note": "grader false-negative cluster; NON-SAT in fts (2 -> 1)."}
{"query": "I want to stand up a second SQLite database for a new feature — how do people do it here, and what bites?", "expect_any": ["auth-DB template", "metadata = MetaData", "global SQLModel"], "intent": "build", "note": "separate-DB pair; the only case auto leaves non-saturating (rank 5) — routing does NOT fix it, documenting a real limit."}
{"query": "To get exactly what the trace UI shows for a session, which call reproduces it?", "expect_any": ["merge_spans"], "intent": "explain", "note": "explain->fact vs grafting lessons; saturates both modes — regression guard that routing must not break."}
